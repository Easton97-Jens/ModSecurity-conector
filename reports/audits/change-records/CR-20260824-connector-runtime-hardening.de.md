# Change Record

**Sprache:** [English](CR-20260824-connector-runtime-hardening.md) | Deutsch

## Identität

| Feld | Wert |
| --- | --- |
| Change-ID | CR-20260824-connector-runtime-hardening |
| Datum (UTC) | 2026-08-24 |
| Basis-Revision | 5d71be74369123257851eb5ec612d7523a6b061d |
| Repository-Grenze | Nur Parent; Framework, MRTS, Gitlink, CI, Branch-Regeln, Rulesets und Required Checks unverändert |
| Evidence | Frühere aufbewahrte Runtime-Evidence plus finale lokale Validierung `/root/git/ModSecurity-conector/.codex/runs/20260825T161923Z-connector-runtime-hardening-final/evidence/final-local-validation.md` (SHA-256 `61b9abdb709104e538e1d56d06afd374b459b0e5d06b5b2735d2863262f10765`) und versiegelter Security-Review-Report (SHA-256 `974516d3bb25d102892091e9492b03935acbe1e99bf0d8c62ba07a0637876b42`) |
| Delivery-Status | Benutzerautorisierter Draft-PR wartet auf finalen Exact-Path-Commit, normalen Push und PR-Erstellung; kein Merge |

## Motivation und Problemstellung

Die Aufgabe verlangte ein einheitliches, explizites Fehler-, Verfügbarkeits- und Cleanup-Modell für Apache, NGINX, HAProxy HTX, HAProxy SPOE/SPOP, Envoy `ext_authz`, Envoy `ext_proc`, Traefik `forwardAuth`, Traefik Native UDS, lighttpd Stock und lighttpd Patched. Ein fehlerhafter oder nicht erreichbarer Engine-, Peer-, Backend-, Client-, Stream-, Body- oder Handshake-Zustand darf keinen langlebigen Connector stillschweigend beenden, spätere Verarbeitung dauerhaft blockieren, unbegrenzt Ressourcen halten oder Prozesse, Ports, UDS-Dateien, Streams oder Transaktionszustände zurücklassen.

## Akzeptanzkriterien

- Direkte libmodsecurity-C-API-Aufrufe behandeln exakt `1` als Erfolg und schlagen bei `0` oder negativen Rückgaben in den abgedeckten Apache-, NGINX-, HAProxy- und gemeinsamen Runtime-Pfaden fail-closed fehl.
- HAProxy-SPOE/SPOP-Schreibvorgänge sind signalsicher und werden ausgewertet; Peer-Reset/EPIPE, unvollständiges oder langsames HELLO, parallele Peers, Folge-HELLO sowie Allow-/Block-Kontrollen haben begrenzte, isolierte Ergebnisse. Eine verlorene zustandsbehaftete Response-Transaktion liefert explizit geschlossen `deny`/`503`, niemals still `pass`/`200`.
- HAProxy SPOP weist einen positiven nicht unterstützten `response-body-timeout` ab, statt eine nicht erzwungene Stream-Deadline zu behaupten; Selftest-Metadaten sind atomar aufruf-eigen und werden bereinigt, ohne aufrufer-eigene Pfade zu ändern.
- HAProxy SPOP weist jede Response-Phasen-Aktivierung vor dem Produktionsstart ab, bis eine Ownership-erhaltende begrenzte P3/P4-Brücke existiert; eine request-side-only-Konfiguration bleibt die legitime Kontrolle.
- Die gemeinsame HTTP-Autorisierung für Envoy `ext_authz` und Traefik `forwardAuth` begrenzt Worker-Aufnahme, überlebt das Schließen eines fehlerhaften Peers und erlaubt nachfolgende Allow-/Block-Anfragen.
- Envoy `ext_proc` trennt Engine-Timeout und Stream-Idle-Timeout, definiert Aktivität pro vollständiger Anfrage, begrenzt parallele Streams, propagiert Cancel und gibt Zustand frei.
- Traefik Native UDS begrenzt Worker-/Socket-Tracking, verwendet eine eigene monotone 30-Sekunden-Peer-Output-Write-Deadline, behandelt Reset und Shutdown und verwendet einen definierten kontrollierten Restart-Pfad, wenn ein nicht unterbrechbarer Engine-Worker nicht entleert werden kann.
- lighttpd Stock und Patched kompilieren und bestehen Baseline-, Block-, Event- sowie Listener-/Cleanup-Smokes; die Stock-ABI zeichnet nur `native-lighttpd-plugin` und die Patched-ABI nur `patched-native-lighttpd` auf.
- Gepatchtes lighttpd erhöht den Response-Hook-Record um `sizeof(*plfd)`; benigne/P2-Marker-Request-TCP-RSTs überleben, und nur die exakte roh disruptive Request-Body-Limit-Signatur mappt `403` auf `413`.
- Keine CI-Workflow-, Branch-Regel-, Ruleset-, Required-Check-, Framework-, MRTS- oder Parent-Gitlink-Datei wird geändert.

## Implementierungsentscheidung und Begründung

- Fehlerhafter, unvollständiger, abgelaufener, zurückgesetzter oder abgebrochener Engine-/Protokollzustand wird bei unentscheidbarer Autorisierung fail-closed behandelt; eine legitime Folgeanfrage wird nach Peer-lokalem Cleanup zugelassen. Host-Aktion, Status, Event, Cleanup und Betreiberwirkung stehen in Runtime-Policy und aufbewahrter Evidence.
- Signalsicherheit gilt lokal je Socket-Schreibvorgang (`MSG_NOSIGNAL`, mit verfügbarer Plattformalternative); globales Ignorieren von SIGPIPE wird nicht verwendet.
- Admission und Shutdown sind begrenzt. Cleanup ist idempotent oder ownership-isoliert; Zustand wird nicht freigegeben, solange ein nicht unterbrechbarer Worker ihn besitzt.
- Die vom Projekt gelieferte HAProxy-SPOE-Konfiguration mit geschlossenem Default setzt `option continue-on-error` nicht: Dieses HAProxy-Opt-in ist mit `fail-mode=closed` inkompatibel, weil ein Agent-Fehler sonst als Allow enden kann. Ein Admission-Close ohne ACK ist ein geschlossener SPOP-Transportfehler; ein Failure-ACK wird auf `503` abgebildet. Der genaue native-HAProxy-Clientstatus für ein unbestätigtes Admission-Close bleibt `NOT_EXECUTED`, während die Peer-lokale Admission-/HELLO-Isolation erhalten bleibt.
- Ein Response-seitiger SPOP-Cache-Korrelationsfehler bleibt im Source-Response-Pfad getrennt fail-closed, auch wenn ein Betreiber gewöhnliche Enginefehler mit `fail-mode=open` gewählt hat: begrenzte Eviction kann ein Verfügbarkeitslimit bleiben, aber ein späteres Response-NOTIFY liefert `deny`/`503`, `disruptive=1` und `stateful_response_transaction_missing_closed`, statt Response-Enforcement still zu überspringen. Die Produktionsgrenze weist Response-Phasen beim Start ab; ihr peer-lokaler Guard weist ein inkompatibles Response-NOTIFY vor der Cache-Verarbeitung mit `response_phase_disabled_closed` ab.
- Der ausgewählte SPOP-Response-Pfad hat keinen Response-Body-Stream. Positiver `response-body-timeout` wird deshalb beim Parsen mit Exit `2` abgewiesen; Zero/Default und per-Frame-`spoe-timeout` bleiben getrennt. PID-, Ready- und Port-Metadaten nutzen `O_CREAT|O_EXCL` (und `O_NOFOLLOW`, falls verfügbar) sowie ownership-maskierten Cleanup, sodass eine Kollision keinen aufrufer-eigenen Pfad überschreiben oder entfernen kann.
- Die Produktions-SPOP-Grenze weist `response-body-limit > 0`, `enable-response-headers` und `response-phases` vor Listener- oder Worker-Start ab, weil das aktuelle request-side-Protokoll keine begrenzte Ownership-erhaltende P3/P4-EOS-Brücke besitzt. Das ist ein geschlossener Konfigurationsfehler, niemals ein impliziter partieller Response-Enforcement-Modus; ein gültiger request-side-only-Start bleibt unterstützt.
- `ext_proc` setzt Aktivität nur nach vollständiger Processing-Request und Response-/Engine-Arbeit zurück. Der allgemeine Engine-Timeout ist kein Stream-Idle-Timeout.
- Das aktuelle `ext_proc`-Follow-up-Fixture prüft nach Rückkehr des Idle-Handlers `pendingReceives == 0`; Mutex- und Forced-Stop-Wartezeiten sind durch Deadlines begrenzt. Ein bereits laufender, nicht unterbrechbarer nativer C-Destruktor nutzt einen kontrollierten Nonzero-Restart-Pfad, nicht eine In-Process-Cancel-Zusage.
- Native-UDS-RESULT-Writes verwenden eine `CLOCK_MONOTONIC`-Peer-Deadline mit `poll(POLLOUT)` und nicht-blockierendem `MSG_NOSIGNAL | MSG_DONTWAIT`. Ablauf schließt nur diesen Peer und gibt seinen Worker frei; das ist kein Engine-Operations- oder Receive-Timeout.
- lighttpd-Response-Start-Helfer werden außerhalb des Patched-Host-ABI-Guards kompiliert, damit beide Hostvarianten denselben Cleanup-sicheren Pfad nutzen.
- Die rohe lighttpd-Event-Identität wird an derselben Compile-Time-ABI-Grenze ausgewählt: Stock-Header wählen `native-lighttpd-plugin`, Streaming-Hook-Header wählen `patched-native-lighttpd`. Ein Stock-Run kann sich damit nicht als Patched-Host-Evidence ausgeben.
- Gepatchtes lighttpd verwendet `resp_fn_step += sizeof(*plfd)`, keine Ein-Byte-Inkrementierung. Die Common Runtime ändert nur die exakte roh disruptive Request-Body-Intervention (`403`, kein Redirect, exaktes Body-Limit-Log) auf `413`; gewöhnliche gleichlautende `403`- und `451`-Rules behalten ihren expliziten Status.

## Security-Auswirkung

Die Änderungen härten Trust-Boundary-Übergänge zwischen Hosts, Clients, Peers, Engines, Sockets, Protokollstreams und Transaktionszustand. Sie reduzieren Crash-, Deadlock-, Ressourcenerschöpfungs-, Request-Blockierungs-, veraltete-Listener- und mehrdeutige-Autorisierungszustände. Allow- und Block-Kontrollen bleiben verfügbar und wurden nach fehlerhaften Peer- sowie Timeout-/Cancel-Fällen erneut geprüft. Die Evidence besteht aus lokaler Source-, Service-, Connector- und Host-Smoke-Evidence; sie beweist nicht jede Produktionshost-, HTTP/2-, HTTP/3-, Reload- oder externe Deployment-Kombination.

## Geänderte Dateien

- `common/runtime/msconnector_runtime.c` — exakte libmodsecurity-Transaktions-Erfolgsprüfungen und enge Request-Body-Limit-`413`-Normalisierung.
- `common/runtime/http_authorization_service.c` — signalsichere Writes, begrenzte Worker-Aufnahme und begrenztes Shutdown-Ownership.
- Apache: `connectors/apache/src/mod_security3.c`, `connectors/apache/src/msc_filters.c` — fail-closed Transaktions- sowie Response-/Body-Prüfungen.
- NGINX: `connectors/nginx/src/ngx_http_modsecurity_access.c`, `ngx_http_modsecurity_body_filter.c`, `ngx_http_modsecurity_header_filter.c`, `ngx_http_modsecurity_module.c` — exakte C-API-Rückgabebehandlung.
- HAProxy: `connectors/haproxy/src/haproxy_modsecurity_binding.c`, `connectors/haproxy/src/haproxy_spop_diagnostic_runtime.c`, `connectors/haproxy/harness/run_haproxy_spop_cache_miss.sh`, `examples/haproxy/compatibility-spoe/modsecurity-agent.conf` — HTX-Prüfungen, isolierte signalsichere SPOE/SPOP-Behandlung, eine request-only Response-Phasen-Guard-Kontrolle, erhaltenes Source-Level-Cache-Miss-Fail-Closed-Verhalten, Parser-Ablehnung eines nicht unterstützten Response-Body-Timeouts und ownership-sicheres Selftest-Metadaten-Cleanup.
- Envoy: `connectors/envoy/ext_proc/cmd/msconnector-envoy-ext-proc/main.go`, `connectors/envoy/ext_proc/internal/processor/config.go`, `processor.go`, `processor_test.go`, `connectors/envoy/config/envoy-ext-proc-service.json`, `examples/envoy/minimal/envoy-ext-proc-service.json`, `examples/envoy/safe/envoy-ext-proc-service.json` — Idle-/Admission-/Cancel-/Shutdown-Kontrollen und Config.
- Traefik: `connectors/traefik/src/traefik_engine_service.c`, `connectors/traefik/build/test-engine-service-runtime.sh` — begrenztes Native-UDS-Drain, kontrollierter Restart und Runtime-Regression für nicht lesende Peer-Write-Deadline.
- lighttpd: `connectors/lighttpd/module/mod_msconnector.c`, `connectors/lighttpd/patches/0001-lighttpd-msconnector-stream-hooks.patch`, `connectors/lighttpd/tests/test_patched_host_contract.py` — Stock-/Patched-Helper-Scope, ABI-korrekte rohe Event-Identität, exakte Callback-Stride-Korrektur und Regression.
- Regressionstests: `tests/test_apache_fail_closed.py`, `connectors/nginx/tests/test_fail_closed_contract.py`, `tests/test_native_api_fail_closed_contract.py`, `tests/test_haproxy_spop_peer_isolation_contract.py`, `tests/test_haproxy_spop_transaction_cache_contract.py`, `tests/test_haproxy_spop_response_timeout_contract.py`, `tests/test_haproxy_spop_selftest_cleanup_contract.py`, `tests/test_modsecurity_request_body_limit_status_contract.py`, `tests/test_http_authorization_service_worker_contract.py`, `tests/test_http_authorization_service_runtime.py`, `tests/test_traefik_engine_service_shutdown_contract.py`, `tests/test_traefik_native_local_plugin.py`.
- Keine CI-, Framework-/MRTS-, Gitlink-, Dependency-, Branch-Regel-, Ruleset- oder Required-Check-Datei gehört zu diesem Record.

### HTX-Append-Remediation-Delta

- `connectors/haproxy/htx-overlay/haproxy_modsecurity_htx_filter.c` liefert nach transaktionslokalem Request- oder Response-nativem Append-Fehler jetzt `-1`; nur eine erfolgreich inspizierte Slice liefert ihre positive Länge.
- `tests/test_haproxy_htx_payload_fail_closed_contract.py` ergänzt den Request-/Response-Callback-Vertrag.
- Der aufbewahrte HAProxy-`3.2.22`-Host-Run reproduzierte das ursprüngliche Request-/Response-Forwarding (`200`) und beobachtete danach gepatchtes Request-`400` mit null Backend-Dispatches sowie gepatchtes Close des betroffenen Response-Streams (`000`, `curl_exit=52`). Beide Fälle bedienten einen legitimen Same-Process-Allow-Control und gaben ihre exakten task-eigenen Listener frei.
- Dies ist FND-PARENT-0946: ein lokaler Security-Fix mit HTTP/1.1-Evidence, kein verifiziertes H2/H3-, Reload-, Full-FD- oder Exact-delivered-head-Ergebnis.
- Tatsächliche Prüfungen: `python3 -m unittest tests/test_haproxy_htx_payload_fail_closed_contract.py` bestand (2 Tests); die fokussierte 22-Test-HAProxy-Helper-/Binding-Suite bestand; der Rebuild des Overlays mit `connectors/haproxy/htx-overlay/build-overlay.sh` gegen aufbewahrtes HAProxy `3.2.22` bestand. Der aufbewahrte Run `haproxy-htx-append-failure-20260825T131500Z` hat SHA-256 `12e4d30c68ff46f45f2f8481d810eb53099f6512f384520e3942fadb0434da9c`.

## Tests und tatsächliche Ergebnisse
### Finales Runtime-Härtungsdelta — 2026-08-25

- Envoy `ext_proc` behandelt ein abgelaufenes natives Transaction-Cleanup jetzt als terminal: Es nimmt keine wiederverwendbare Arbeit mehr an, meldet dem Prozessbesitzer genau einen Fatal-Fehler, erzwingt begrenzten gRPC-Stop und endet für den Supervisor mit Nonzero. Dies ist getrennt vom dokumentierten Stream-Idle-Timeout.
- HAProxy SPOP weist jetzt einen typisierten Peer-Wert zurück, der einschließlich NUL-Terminator nicht passt, statt ihn still abzuschneiden. Header-Anzahl/Name/Value/gesamte Größen sind vor Allokation begrenzt; `spoe-timeout`, `worker-count` und `max-transactions` verwenden die strikten Dezimalbereiche `1..60000`, `1..64` und `1..4096` bei höchstens `65536` gesamten Transaktionsslots.
- Apache- und lighttpd-Harness-Ergänzungen machen Prozess-/Listener-Cleanup identity-bound und bei unsicherer Ownership fail-closed; HTX-Payload-Append-Fehler stoppen jetzt den betroffenen Stream, statt einen positiven erfolgreichen Append zu behaupten.
- Die fokussierte aktuelle Validierung bestand 237 Tests mit 2 erwarteten Skips, `make -C connectors/haproxy check-htx-overlay` und `git diff --check`. Der finale versiegelte Security-Diff-Review enthält null reportable Findings und partielle Coverage.
- Der Draft-PR bleibt bewusst partiell: vollständige native 17-Vektor-mal-10-Connector-Host-Coverage, exakte nicht unterbrechbare CGo-Host-Reproduktion und die zwei benannten Proof-Gaps bleiben offen. CI wurde weder geändert noch ausgeführt.


Die vollständigen aufbewahrten Befehle und beobachteten Ergebnisse stehen in der oben genannten Evidence-Datei.

- Apache-Contracts: 4 bestanden; das aktuelle Source-Modul baute gegen Apache 2.4.66/APXS und frühere isolierte echte Host-Controls lieferten Allow `200`, P1-Block `403` und P2-Block `403`. Eine neue begrenzte V7-Diagnose lieferte Proxy-`502`, aber ihr Inline-Follow-up-Backend war fehlerhaft geframt und Cleanup nicht zuordenbar: Die Task-Ports `29471/29472` bleiben nach dem exakten Stop-Versuch ohne sichtbare PID im Listen. Kein unsicheres Signal wurde gesendet. Apache V7/V16/V17 bleiben daher unter FND-HOST-0007 `BLOCKED_ENVIRONMENT`; die optionalen synchronisierten `engine-limit`- und `client-abort`-Fixtures waren getrennt vor der Upstream-Adressveröffentlichung blockiert.
- NGINX-Contracts: 13 bestanden, einschließlich der fail-closed-Regression Zero-Return zu 500. Die aktuelle Source baute gegen gepinntes NGINX 1.31.4. Ein begrenzter nativer V7-Host beobachtete einen kontrollierten Upstream, der 21 von deklarierten 128 Byte sendete; NGINX loggte das frühe Close, der Client sah committed `200`, danach `curl_exit=18` mit 107 fehlenden Byte, NGINX überlebte und Same-Host-Allow/Block/Allow lieferte `200 -> 403 -> 200`. Ein separater begrenzter V6/V10-Upload-Probe sendete 5 von deklarierten 100 Byte und dann FIN oder TCP-RST; der Client war bereits geschlossen, NGINX loggte `400`, der Host überlebte, derselbe Prozess lieferte `200 -> 403 -> 200`, und seine exakte PID/sein Port `29583` sowie temporäre Verzeichnisse wurden entfernt. Ein frischer direkter Native-Host-Parallel-Run bediente 16 parallele Requests (8 Allow `200`, 8 Block `403`) in einem weiterlaufenden Prozess und lieferte danach `200 -> 403 -> 200`; seine exakte PID, Port `29671` und PID-Datei fehlten nach begrenztem Cleanup. Die RST-Upload-Form wird nicht als unabhängiger V9-Nachweis hochgestuft. Begrenzter Cleanup entfernte die V7-PIDs/-Ports `29371/29372`. Das Direct-Mode-Ergebnis deckt keine Worker-Identity ab; der kanonische Sandbox-Worker-Harness bleibt durch `chown(..., nobody)` `EINVAL` blockiert; der separate Port `29183` wurde freigegeben und ein späterer lesender Parent-Recheck fand weder `29182` noch `29183` als Listener.
- Direkte C-API-Contracts: 3 bestanden.
- HAProxy-SPOP-Source-Contracts: 13 fokussierte Prüfungen bestanden, einschließlich Response-Body-Timeout-Ablehnung, peer-lokaler Response-Phasen-Ablehnung und Metadaten-Ownership-/Collision-Controls. Der aktuelle Production-Binary wies `--response-body-timeout 25` mit Exit `2` ab; sein Selftest meldete `metadata_cleanup: PASS` und hinterließ keine Metadaten. Die aktuelle request-only `run_haproxy_spop_cache_miss.sh`-Kontrolle mit `max-transactions=1` beobachtete Response-Phasen-`deny`/`503`/`response_phase_disabled_closed`, echten Rule-Block `403`, frisches Allow `200`, Agent-Cleanup und einen fehlenden dynamischen Listener. Der ältere Cache-Miss-Pfad `stateful_response_transaction_missing_closed` bleibt Source-Level-Evidence. GCC-/Clang-Runtime-Self-Tests, Reset/EPIPE, Peer-Isolation, sofortiges Schließen eines gesättigten Peers, HELLO-Deadline und Folgekontrollen bestanden ebenfalls.
- HAProxy-HTX-Overlay-Checks und Helper-Suite (11 Tests) bestanden. Ein frischer begrenzter nativer Host-P1--P4-Run lieferte Allow `200`, P1-Precommit-Ablehnungen `403` und `429`, P2-Client-Body-Ablehnung `403` vor Upstream-Dispatch, P3-Response-Header-Ablehnung `403` nach einem Upstream-Request und P4-Late-Committed-Response-Safe-Log-Only `200` mit dem ersten Client-Byte vor Upstream-EOS. Er zeichnete `processes_stopped=yes`, keinen task-eigenen Frontend-/UDS-Listener nach Cleanup und `capability_promotion=not_permitted` auf; dies ist nur Evidence für diese Controls, keine vollständige 17-Vektor- oder Response-Bridge-Abnahme.
- Envoy-Builds/Configs bestanden. Die `ext_authz`-Runtime bestand fehlerhafte-Peer-Recovery, `200` Allow, `403` Block, begrenzten Exit und Listener-Cleanup.
- Envoy-`ext_proc`-Go-Unit- und `-race`-Suites sowie der getaggte native CGo-Test bestanden, einschließlich Timeout, Cancel, Shutdown, Admission-Freigabe, Folgekontrollen und `TestCommonRuntimeEngineCloseHonorsShutdownContext`; alle drei Config-Checks bestanden.
- Traefik-`forwardAuth`-Config/Runtime bestanden fehlerhafte-Peer-Recovery, `200` Allow, `403` Block, Exit und Port-Cleanup.
- Traefik Native UDS bestand GCC, Clang und ASan/UBSan für Protokoll, Reset, Folgeanfrage, Ownership und Cleanup ohne Sanitizer-Diagnose. Die aktuelle Native-Service-Regression füllte alle 64 Worker mit nicht lesenden UDS-Peers, beobachtete vor Ablauf keine bediente Folgeanfrage, danach bei 31,0 Sekunden eine frische Anfrage und vollständiges Socket-/Prozess-Cleanup; derselbe Fall bestand unter ASan/UBSan.
- Fokussierte lighttpd-Contracts bestanden (44 Tests, 2 erwartete Skips). Die exakten gepatchten 1.4.85-Normal- und ASan/UBSan-Hosts überlebten benigne/P2-Marker-partielle TCP-RSTs ohne synthetischen P2-Abbruch oder Sanitizer-Diagnostik; Empty-/32-Byte-Allow `200`, gewöhnlicher P2-`403`, gleichlautende gewöhnliche `403`/`451`, 33/64-Byte-Limit `413`, unabhängiges Follow-up, Event-Evidence, PID- und Listener-Cleanup bestanden.
- Die aktuelle Dual-ABI-lighttpd-Validierung bestand 37 fokussierte Contracts (2 erwartete Namespace-Skips), baute dasselbe Modul mit C17 gegen aufbewahrte Stock- und Patched-1.4.85-Header neu und beobachtete für beide Varianten `200` Allow, `403` Block und danach `200` Same-Process-Allow. Rohe Stock-Events enthalten nur `native-lighttpd-plugin`, Patched-Events nur `patched-native-lighttpd`, und beide task-eigenen Listener-/PID-Prüfungen waren sauber.
- Begrenzte lighttpd-Backend-Close-Receipts deklarierten 64 Byte, sendeten nur `short` (5 Byte) und lieferten einen fünfsekündigen Frontend-Timeout. Die späteren Controls und die finale PID-/Listener-Abwesenheit bleiben aufbewahrt, aber keine Variante beobachtete Frontend-EOF/definierten Fehler, ein Peer-Error-Event oder unmittelbares Stream-/FD-/Transaktions-Cleanup vor dem expliziten Host-Stop. Weder Stock noch Patched V7/V11 wird hochgestuft. Die Wiederholung mit aktuellem ABI-korrektem Stock-Modul prüft `d1429392...` und `native-lighttpd-plugin`, hat aber dieselbe Closure-Evidence-Lücke. Der ursprüngliche Receipt ist unter `/var/tmp/codex/ModSecurity-conector/runs/20260824T120000Z-connector-runtime-hardening/runtime-continuation/lighttpd-backend-close-20260824T230000Z/validation.md` aufbewahrt (SHA-256 `ef460a10515fbdb6ff994f0d57ffb2f13c39d4eae1debd6e39eca6088bcc4a4d`); die unabhängige Korrektur ist unter `/var/tmp/codex/ModSecurity-conector/runs/20260824T120000Z-connector-runtime-hardening/runtime-continuation/lighttpd-stock-current-close-20260824T233000Z/closure-review-addendum.md` aufbewahrt (SHA-256 `d43e72ab55fca997c9b840c01de4cd90aa9f0d300273138bdde7776e1d84e10d`). FND-PARENT-0311 verfolgt den erforderlichen rohen Upstream-/Frontend-Closure-Harness.
- Ein separates aktuelles Stock-Raw-V7/V11-Lifecycle-Fixture committe HTTP-`200` und einen deklarierten 64-Byte-Body, sendete fünf Body-Bytes und schloss dann; begrenzter Host-/Transport-Harness und seine Cleanup-Receipts bestanden. Dies bleibt absichtlich begrenzte Evidence: Sie behauptet kein typisiertes Stock-Connector-Event, keine direkte Transaktionszustandsfreigabe, keinen vollständigen FD-/Leak-Audit und keine vollständige V7/V11-Abnahme. Der entsprechende vollständige Frontend-Close-/Transaktions-Cleanup-Nachweis für Patched-lighttpd bleibt offen.
- Die Evidence wurde wie unter Identität angegeben aufbewahrt und gehasht.

## Ausgeführte Befehle

Die aufbewahrte Evidence enthält die exakten Befehle und Ergebnisse. Die
wichtigsten lokalen Prüfungen waren die Connector-Contract-Suites, die
HAProxy-SPOE/SPOP-Runtime-Self-Tests, `go test ./...` und `go test -race ./...`
für Envoy, der getaggte native Envoy-Test, GCC-/Clang-/ASan-/UBSan-Läufe für
Traefik Native UDS, frische lighttpd-Stock-/Patched-Smokes, `make
check-doc-links` und `make check-bilingual-docs`.
Die reproduzierbare SPOP-Cache-Kontrolle lautet
`BUILD_ROOT=<task-eigener-build> SPOA_BIN=<aktueller-agent> RUNTIME_ROOT=<task-eigene-runtime> connectors/haproxy/harness/run_haproxy_spop_cache_miss.sh`.

## Runtime-Evidence

Die aufbewahrte Evidence deckt Engine-Startverfügbarkeit, Fehler innerhalb einer Transaktion, Timeout, ungültige/unvollständige Peer-Eingaben, Client-/Peer-Reset, unvollständige HELLO- und Body-/Stream-Grenzen, soweit der lokale Harness sie bereitstellt, parallele Requests/Streams, Größen-/Admission-Grenzen, Cancel, Shutdown, Folgekontrollen und Cleanup ab. SPOP-Logs erfassen Reset `errno=104`, EPIPE `errno=32`, explizite Ablehnung des nicht unterstützten Timeouts, atomaren Metadaten-Cleanup und den aktuellen Cache-Miss `503`, gefolgt von echten Block- und Allow-Kontrollen. Der begrenzte aktuelle HAProxy-HTX-Host-Run erfasst P1 `403`/`429`, P2-`403` vor Upstream, P3-`403` und committed P4-Safe-Log-Only `200` ohne Full-Response-Buffering; er ist ausdrücklich `native_host_runtime_nonpromoted`. Die begrenzte NGINX-V7-Evidence bewahrt Early-Close-Log, committed-Status-Transportfehler, Same-Host-`200 -> 403 -> 200` sowie PID-/Port-Cleanup auf; ihr V6/V10-Receipt bewahrt unvollständige FIN-/RST-Uploads, serverseitiges `400`, Prozessüberleben, Folgekontrollen sowie Port-/Tempdir-Cleanup ohne unabhängigen V9-Claim auf. Ein frischer NGINX-Run im Direct-Mode bewahrt zusätzlich 16 parallele Allow-/Block-Ergebnisse, Same-Process-Controls und PID-/Port-Cleanup auf, aber keine Worker-Identity-Semantik. Apache bewahrt frühere Allow/Block-Controls und ein diagnostisches V7-`502`, aber sein V7-Follow-up/-Cleanup ist wegen nicht zuordenbarer Listener bewusst blockiert statt hochgestuft. Gepatchtes lighttpd bewahrt Normal- und ASan/UBSan-Partial-RST-, exakte-`413`-Status-, Event-, PID- und Listener-Evidence auf. Beide begrenzten lighttpd-Backend-Close-Receipts bleiben partiell/nicht hochgestuft, weil sie timeouteten statt Frontend-Abschluss und unmittelbares Cleanup vor Host-Stop zu belegen; FND-PARENT-0311 bewahrt die genaue Lücke auf. Der Native-UDS-Test prüft nach Reset und 64-Peer-Write-Deadline-Erschöpfung eine legitime Folgeanfrage sowie das Fehlen des zugewiesenen UDS-Listeners.

Das separate aktuelle Stock-Raw-V7/V11-Lifecycle-Fixture beobachtete einen
abgeschnittenen 64-Byte-Upstream-Body nach fünf Byte und begrenztes
Host-/Transport-Cleanup. Es ersetzt weder die vollständige
Frontend-/Transaktionsfreigabe-Anforderung von FND-PARENT-0311 noch behauptet
es ein typisiertes Stock-Event oder einen vollständigen Leak-Audit.

## Nicht ausgeführte Prüfungen mit Begründung

- Der begrenzte HAProxy-HTX-P1--P4-Native-Host-Control-Run ist verfügbar, aber native V7-Early-Close-/RST-, V6/V9-Transportfehler-, V12--V15-Lifecycle-/Limit-/Parallelitäts- und vollständige 17-Vektor-Abnahme bleiben ungetestet. NGINX hat das obige begrenzte V7-Native-Host-Ergebnis, aber sein kanonischer Worker-Harness bleibt sandbox-blockiert. Apache hat nur frühere begrenzte Controls; sein neuester V7-Run bleibt blockiert, bis Listener-Ownership und Cleanup beobachtbar sind.
- Das begrenzte aktuelle Stock-Raw-V7/V11-Fixture lief, ist aber nur Host-/Transport-Evidence und belegt weder ein typisiertes Connector-Event noch direkte Transaktionsfreigabe oder einen vollständigen FD-/Leak-Audit. Patched-lighttpd V7/V11 benötigt weiterhin rohen Upstream-Close, Frontend-EOF/definierten Fehler, unmittelbares Stream-/FD-/Transaktions-Cleanup und Same-Host-Post-Fault-Controls vor einer Hochstufung.
- Kein nativer Envoy-Proxy- oder Traefik-Proxy-Host wurde gestartet; Connector-/Agent-Binaries und gemeinsame Services wurden ausgeführt.
- Ein absichtlich hängender libmodsecurity-Engine wurde nicht unsicher simuliert; begrenzter Shutdown und kontrollierter Nonzero-Restart wurden durch Source/Tests geprüft.
- Der Default-Pfad von Traefik `check-config` versuchte das historische globale `/var/tmp/ModSecurity-conector-verified/logs` zu erstellen und wurde durch die Sandbox blockiert. Eine task-eigene Konfiguration und `event_path` bestanden; kein globaler Pfad wurde erstellt.
- Vollständige Native-Host-, HTTP/2/HTTP/3-, Reload-, Cross-Connector-Leak- und ThreadSanitizer-Matrizen wurden nicht ausgeführt. CI wurde bewusst weder ausgeführt noch geändert.

## Bekannte Einschränkungen

Native Host-Integration sowie produktionsspezifisches Reload-, TLS-, HTTP/2-, HTTP/3- und Langzeit-Scheduling-Verhalten bleiben umgebungsabhängig. Der kontrollierte Restart-Zweig ist eine explizite Verfügbarkeitsentscheidung für einen nicht unterbrechbaren Engine-Worker; er behauptet nicht, dass ein solcher Worker ohne Prozessrestart wiederhergestellt werden kann. Die aufbewahrte Evidence ist starke lokale Evidence, aber kein vollständiger Hosted-Matrix-Nachweis.

## Verbleibende Risiken

Betreiber müssen weiterhin connector-spezifische Limits, Timeouts, TLS-/UDS-Berechtigungen, Reload-Reihenfolge und Monitoring in nativen Hostversionen validieren. Nicht verfügbare Native Hosts und nicht ausgeführte Matrizen bleiben Release-Readiness-Einschränkungen, bis sie unabhängig reproduziert wurden. Keine CI- oder Governance-Schutzmaßnahme wurde abgeschwächt.

## Finaler Diff- und Review-Status

Die Implementierungs-Evidence ist aufbewahrt, auf Parent begrenzt und mit diesem bilingualen Change Record gepaart. Die Remediations für NGINX-Zero-Return, gesättigte HAProxy-SPOP-Aufnahme/Response-Cache/Timeout/Metadaten, nicht lesende Traefik-Native-UDS-Peers, nativen Envoy-`ext_proc`-Shutdown, Common-Runtime-`413` und gepatchten-lighttpd-Callback-Stride sind lokal behoben; ihre Findings bleiben offen oder `fixed` statt geschlossen, wo Native-Host-/FD-Vektor-Evidence noch fehlt. Dieser Record führt keinen Commit, Push, PR, Merge oder Finding-Abschluss aus. Die finale Auslieferung und ein etwaiger Draft-PR benötigen die separate scoped Diff-Prüfung und Delivery-Policy-Checks des Parent-Agents.
