name: Five Connector No-CRS Baseline

on:
  workflow_dispatch:
  schedule:
    - cron: '23 3 * * 3'

permissions: {}

jobs:
  no-crs:
    permissions:
      contents: read
    uses: ./.github/workflows/reusable-five-connectors-profile.yml
    with:
      profile: no-crs
