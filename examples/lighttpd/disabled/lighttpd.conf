# Disabled native lighttpd reference. The module may be loaded, but disabled
# prevents Common Runtime startup and requires no msconnector.config-file.
server.compat-module-load = "disable"
server.modules = ( "mod_msconnector" )
server.document-root = "/srv/lighttpd/htdocs"
server.bind = "127.0.0.1"
server.port = 8080
server.errorlog = "/var/log/lighttpd/error.log"
server.pid-file = "/run/lighttpd/lighttpd.pid"
server.upload-dirs = ( "/var/tmp/lighttpd-upload" )
msconnector.enabled = "disable"
