# NGINX Build

Status: scaffolded

Observed local source uses the NGINX third-party module `config` file.
The source README documents `--add-module` and `--add-dynamic-module`.

No NGINX build is implemented in this repository.

TODO:

- Verify supported NGINX versions.
- Decide static vs dynamic module support.
- Add CI only after a local build is reproducible.
