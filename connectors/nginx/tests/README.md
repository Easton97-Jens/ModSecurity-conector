# NGINX Connector Tests

Status: scaffolded

NGINX-specific tests belong here.

Tests that depend on nginx-tests, NGINX configuration, filter ordering, HTTP/2
handling, or NGINX log format must not be placed in `tests/common/`.
