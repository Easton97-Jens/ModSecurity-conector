# NGINX Connector

Status: scaffolded

This directory is reserved for an NGINX adapter for libmodsecurity v3.

Implemented now:

- Documentation of observed local NGINX connector concepts.
- Directory layout for future source and connector-specific tests.

Not implemented:

- No NGINX module source.
- No static or dynamic module build.
- No runtime or test execution.

Primary local reference: `/root/conecter/ModSecurity-nginx`.
