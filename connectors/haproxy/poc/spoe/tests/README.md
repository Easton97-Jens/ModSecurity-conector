# SPOE PoC Tests Placeholder

documentation only

Dieses Verzeichnis beschreibt nur geplante Minimaltests (planned only). Es
enthält keine Testimplementierung.

## Geplante Minimaltests (alle planned only)
- `haproxy_config_syntax` — planned only
- `haproxy_startup` — planned only
- `spoa_component_startup` — planned only
- `benign_request_allowed` — planned only
- `malicious_request_block_signal` — planned only
- `logs_emitted` — planned only
- `report_generated` — planned only

## Offene Punkte
- Exakte Testdaten/-erwartungen: Noch zu prüfen.
- Runtime-Aussagekraft je Test: Noch zu prüfen.
- Vollständige Abdeckung der ModSecurity-Semantik: Nicht belegbar aus dem aktuellen Repository.
