# HAProxy Connector Tests

Status: scaffolded

HAProxy-specific tests belong here.

SPOE, Lua, native-filter, HAProxy configuration, and proxy log behavior tests
must not be placed in `tests/common/`.
