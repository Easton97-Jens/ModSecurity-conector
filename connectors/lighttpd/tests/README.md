# Lighttpd Connector Tests

Status: scaffolded

Lighttpd-specific tests belong here.

Tests that depend on native plugin hooks, `mod_magnet`, Lighttpd configuration,
or Lighttpd logs must not be placed in `tests/common/`.
