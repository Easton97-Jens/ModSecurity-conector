# Lighttpd Build

Status: unknown

No build is implemented.

TODO:

- If native plugin is selected, document supported Lighttpd version and plugin
  build process.
- If `mod_magnet` is selected, document Lua script deployment and limitations.
